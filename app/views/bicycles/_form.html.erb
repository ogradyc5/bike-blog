<!--%= form_for(@bicycle) do |f| %-->
<% if @bicycle.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@bicycle.errors.count, "error") %> prohibited this bicycle from being saved:</h2>
    <ul>
    <% @bicycle.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class='row'>
  <div class='col-xs-12'>
  <%= form_for(@bicycle, :html => {class: "form-horizontal", role:"form"}) do |f| %>
    <div class="form-group">
      <div class="control-label col-sm-2">
        <%= f.label :title %><br>
        <!--%= f.text_field :title %-->
      </div>
    <div class="col-sm-8">
      <%= f.text_field :title, class: "form-control", placeholder: "Bike Name", autofocus: true %>
    </div>
  </div>
  <div class="form-group">
    <div class="control-label col-sm-2">
      <%= f.label :description %>
    </div>
    <div class="col-sm-8">
      <%= f.text_area :description, rows: 10, class: "form-control", placeholder: "Bike Description", autofocus: true %>
    </div>
  </div>
  <div class="form-group">
    <div class="control-label col-sm-2">
      <%= f.label :price %>
    </div>
    <div class="col-sm-8">
      <%= f.number_field :price, class: "form-control", placeholder: "Price per day", autofocus: true %>
    </div>
  </div>
      <!--%= f.label :description %><br>
      <!--%= f.text_area :description %-->
    <!--/div-->
    <!--div class="field"-->
    <!--p>
      <!--%= f.label :price %><br>
      <!--%= f.text_field :price %-->
    <!--/p-->
    <!--/div>
    <div class="actions"-->
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: 'btn btn-primary btn-lg' %>
    </div>
  </div>
    <!--/div-->
  <% end %>
  <div class="col-xs-4 col-xs-offset-4">
    [ <%= link_to "Cancel request and return to bikes", bicycles_path %> ]
  </div>
  </div>
</div>